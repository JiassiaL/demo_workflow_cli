server:
  port: 8080

spring:
  application:
    name: demo
  datasource:
    url: jdbc:postgresql://localhost:5432/prueba_db
    username: postgres
    password: "@Jiassia1979"
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        "[format_sql]": true
logging:
  level:
    "[org.hibernate.sql]": debug
    "[org.hibernate.type.descriptor.sql.BasicBinder]": trace
